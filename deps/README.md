# Local dependencies

This directory is for local, vendored, or editable dependencies — for example a
library you cloned into the repo instead of installing from PyPI.

It is empty by default. See the "OPTIONAL / LOCAL DEPENDENCIES" section of
`pyproject.toml` for how to register something placed here.
