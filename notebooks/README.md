# Notebooks

Jupyter notebooks where students fill out and run code go here. Each notebook
should pair with a lesson in `lessons/` and import helpers from `src/`.

Launch the notebook server with:

```bash
uv run jupyter notebook
```
